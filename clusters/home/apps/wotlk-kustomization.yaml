apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-wotlk
  namespace: flux-system
spec:
  interval: 10m
  # Suspended until you’re ready to cut over from 1.12.1 (ports 3724/8085 conflict).
  suspend: false
  path: ./apps/wotlk
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
